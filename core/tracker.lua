local plugin_label = 'azmodan_farm'
-- kept plugin label instead of waiting for update_tracker to set it

local tracker = {
    name        = plugin_label,
    drop_sigils = false,
    drop_items = false
}

return tracker